# DP: updates from the 16 branch upto 20260809 (documentation).

LANG=C git diff --no-renames --src-prefix=a/src/ --dst-prefix=b/src/ \
	78d4ac73dd391005b895a6148cd9831e28e1208b 643129fbd5a2bd227fa798ee066098cd5fe99290 \
	| awk '/^diff .*\.texi/ {skip=0; print; next} /^diff / {skip=1; next} skip==0' \
        | grep -v -E '^(diff|index)'

