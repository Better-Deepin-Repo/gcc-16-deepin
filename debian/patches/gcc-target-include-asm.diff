# DP: Search $(builddir)/sys-include for the asm header files

--- a/src/configure.ac
+++ b/src/configure.ac
@@ -3857,7 +3857,7 @@ if test -d ${srcdir}/gcc; then
   fi
 
   # Search for pre-installed headers if nothing else fits.
-  FLAGS_FOR_TARGET=$FLAGS_FOR_TARGET' -isystem $(build_tooldir)/include -isystem $(build_tooldir)/sys-include'
+  FLAGS_FOR_TARGET=$FLAGS_FOR_TARGET' -isystem $(build_tooldir)/include -isystem $(build_tooldir)/sys-include -isystem $(CURDIR)/sys-include'
 fi
 
 if test "x${use_gnu_ld}" = x &&
